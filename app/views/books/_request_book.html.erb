<h1>Request Book</h1>
<% if current_user.requested_book(book) %>
  <%= form_for([book, @new_request]) do |r| %>
    <%= r.label "Date" %>
    <%= r.date_field :request_date %>
    <%= r.submit "Request" %>
  <% end %>
<% else %>
  <span>You have already requested this book, wait for an answer</span>
<% end %>
