<h1 class="main-title">Book List:</h1>
<% @books.each do |book| %>
  <div>
    <%= link_to(book.title, book) + " - #{book.author.first_name} #{book.author.last_name} Published: #{book.year} - ISBN: #{book.isbn}" %>
  </div>
<% end %>
